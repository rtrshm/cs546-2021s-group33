<main class="screen">
    <!-- <a href="/nav">Go back</a> -->
    <button onclick="location.href='/nav'" type="button">Go Back</button>
    {{#if games}}
    <ul>
        {{#each games}}
        <li><a href="/games/game/{{this._id}}">{{this.title}}</a></li>
        {{/each}}
    </ul>
    {{/if}}
</main>